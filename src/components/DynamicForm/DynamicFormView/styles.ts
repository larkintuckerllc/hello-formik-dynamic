import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rootFailed: {
    color: 'red',
    textAlign: 'center',
  },
  rootSucceeded: {
    color: 'green',
    textAlign: 'center',
  },
});

export default styles;
