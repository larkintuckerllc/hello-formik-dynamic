import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rootError: {
    color: 'red',
  },
  rootInput: {
    borderWidth: 1,
    height: 40,
    padding: 10,
  },
  rootLabel: {
    flexDirection: 'row',
  },
});

export default styles;
